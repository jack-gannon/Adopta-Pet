import React from "react";
import Hero from "../Components/Hero";
import Spinner from "../Components/Spinner";

const Home = () => {
  return (
    <div>
      <Hero />
      <Spinner />
    </div>
  );
};

export default Home;
